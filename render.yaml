services:
  - type: web
    name: proyfrontendgrupo06
    env: node
    plan: free
    buildCommand: |
      pnpm install --no-frozen-lockfile --production=false
      pnpm build
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
